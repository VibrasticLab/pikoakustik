#include "driver.h"

int samplerate = 44100;
double frequency = 1760.0;
int durasi = 2;
int amplitudo = 10000;

int main(void){
	system_init();
	driver_init(samplerate);
	sample_prep(frequency,durasi,amplitudo);
	play_wave();

	while(1){ system_loop(); }
	return 0;
}

